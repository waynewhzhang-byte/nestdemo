# ===========================================
# 图书馆管理系统 - 后端环境变量
# 复制为 .env 并填写实际值，生产环境务必更换所有密钥
# ===========================================

# 运行环境: development | production
NODE_ENV=development

# 服务端口
PORT=3000

# API 前缀（与前端 VITE_API_URL 对应）
API_PREFIX=api/v1

# CORS: 允许的前端域名（生产环境必须设置为实际前端地址）
# 示例: https://library.yourschool.edu
FRONTEND_URL=http://localhost:5173

# 数据库连接（生产环境使用强密码，建议添加 connection_limit）
# 生成强密码: openssl rand -base64 24
# 示例: postgresql://user:STRONG_PASSWORD@host:5432/library?schema=public&connection_limit=10
DATABASE_URL=postgresql://postgres:CHANGE_ME@localhost:5432/library?schema=public

# JWT 密钥（生产环境必须使用强随机值，至少 32 字符）
# 生成: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_USE_openssl_rand_base64_32

# JWT 过期时间（access token）
JWT_EXPIRATION=15m

# Refresh token 过期时间（可选，部分实现可能使用固定值）
JWT_REFRESH_EXPIRES_IN=7d

# Bcrypt 盐轮数（10 为推荐值，生产可保持）
BCRYPT_SALT_ROUNDS=10

# 生产环境可选：禁用 Swagger 文档（默认 production 下自动禁用）
# SWAGGER_ENABLED=false

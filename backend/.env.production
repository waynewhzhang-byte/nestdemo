# ===========================================
# 图书馆管理系统 - 生产环境配置
# ⚠️ 请修改所有 CHANGE_ME 为实际值
# ===========================================

# 运行环境
NODE_ENV=production

# 服务端口
PORT=3000

# API 前缀
API_PREFIX=api/v1

# CORS: 生产环境前端域名
FRONTEND_URL=https://library.yourschool.edu

# 数据库连接（生产环境使用强密码）
# 格式: postgresql://user:password@host:port/database?options
DATABASE_URL=postgresql://postgres:CHANGE_ME@localhost:5432/library?schema=public&connection_limit=20&pool_timeout=10

# JWT 密钥（必须使用强随机值，至少 32 字符）
# 生成: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_USE_openssl_rand_base64_32

# JWT 过期时间
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Bcrypt 盐轮数
BCRYPT_SALT_ROUNDS=10

# 禁用 Swagger（生产环境）
SWAGGER_ENABLED=false

# 日志级别
LOG_LEVEL=warn

# 请求超时（毫秒）
REQUEST_TIMEOUT=30000
